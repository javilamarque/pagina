window.onload = function(){
    const createAccountButton = document.getElementById('submit')

    createAccountButton.addEventListener('click', ()=>{
        createAccountButton.addEventListener('d-none')
    })
}